edge asset create:
  parameters:
    cluster_resource_group:
      rule_exclusions:
      - parameter_should_not_end_in_resource_group
    custom_location_resource_group:
      rule_exclusions:
      - parameter_should_not_end_in_resource_group
