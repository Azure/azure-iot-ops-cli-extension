# resource label formats
COMPONENT_LABEL_FORMAT = "app.kubernetes.io/component in ({label})"
NAME_LABEL_FORMAT = "app.kubernetes.io/name in ({label})"

# resource label
HELM_MANAGED_LABEL = "app.kubernetes.io/managed-by in (Helm)"
